// Preload script runs in a context that has access to both DOM and Node.js APIs
// This file can be used to expose safe APIs to the renderer process if needed
// For now, we don't need to expose anything since the app uses standard web APIs

window.addEventListener('DOMContentLoaded', () => {
  // Any initialization code that needs to run before the main script
});

